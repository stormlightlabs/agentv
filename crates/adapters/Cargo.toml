[package]
name = "agent-viz-adapters"
version = "0.1.0"
edition = "2024"

[dependencies]
agent-viz-core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
tracing = "0.1"

[dev-dependencies]
tempfile = "3.15"
